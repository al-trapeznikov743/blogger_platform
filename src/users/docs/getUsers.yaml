summary: Get users
tags: [Users]
security:
  - basicAuth: []
responses:
  200:
    description: List of all users
    content:
      application/json:
        schema:
          $ref: './components.yaml#/components/schemas/UsersViewModel'
  401:
    description: Unauthorized
